library flutter_hk_ui;

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
  /// Returns [value] minus 1.
  int minusOne(int value) => value - 1;
}
